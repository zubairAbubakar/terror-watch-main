version: '3.8'
services: 
    backend:
        build: 
            context: .
            dockerfile: Dockerfile
        ports: 
            - 8001:5000
        volumes: 
            - .:/app
        depends_on: 
            - db

    db:
        image: mysql:5.7.22
        restart: always
        environment: 
            MYSQL_DATABASE: terror_watch_main
            MYSQL_USER: admin
            MYSQL_PASSWORD: p4ssw0rd
            MYSQL_ROOT_PASSWORD: p4ssw0rd
        volumes: 
            - .dbdata:/var/lib/mysql
        ports: 
            - 33067:3306
